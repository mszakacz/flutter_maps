import 'package:flutter/material.dart';

class AppColors {
  static const white = Colors.white;
  static const black = Colors.black;
  static const red = Color.fromARGB(255, 241, 71, 58);
  Color darkColor = Colors.black.withOpacity(0.55);
}
